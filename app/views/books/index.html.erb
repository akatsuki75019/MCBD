<p style="color: green"><%= notice %></p>

    <!-- Product Section Begin -->
<section class="product-page spad">
  <div class="container">
    <div class="row">
      <div class="col-lg-12">
        <div class="product__page__content">
          <div class="product__page__title">
            <div class="row">
              <div class="col-lg-8 col-md-8 col-sm-6">
                <div class="section-title">
                  <%= form_with( url: books_by_category_books_path) do |form| %>
                    
                    <%= form.select( :category_id,options_for_select( @categories.collect {|category| [ category.name, category.id] }))%>
                    <%= form.submit "selectionez svp", class: "btn site-btn" %>
                  <%end%>
                  <h4>Shonen</h4>
                </div>
              </div>
            </div>
          </div>
          <%= render partial: 'books/range' %>
        </div>
      </div>
    </div>
  </div>
</section>


