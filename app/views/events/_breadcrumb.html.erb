<div class="breadcrumb-option">
  <div class="container">
    <div class="row">
      <div class="col-lg-12">
        <div class="breadcrumb__links">
          <%= link_to '<i class="fa fa-home"></i> Home'.html_safe, root_path %>
          <%= link_to "Evènements", events_path %>
          <% if @event.present? %>
            <span><%= @event.title %></span>
          <% end %>
        </div>
      </div>
    </div>
  </div>
</div>