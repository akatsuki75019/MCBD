Bonjour <%= @user.first_name %> ,

Nous vous confirmons votre paiement de <%= @order.total_price %> euros pour votre commande numéro : <%= @order.id %>. 
Celle-ci est actuellement en préparation. Vous pouvez venir la récupérer dès que possible à notre boutique.

Contenu du panier : 

<% @books.each do |book| %>
  <%= book.title %> - Quantité: <%= book.joint_table_order_books.find_by(order: @order).quantity %> - Prix total: <%= book.price_code.price * book.joint_table_order_books.find_by(order: @order).quantity %>
<% end %>

Pour accéder à votre espace client, connectez- via : <%= @url %>.
A très vite !
