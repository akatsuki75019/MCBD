<% if @joint_table_cart_books.present? %>
  <div class="text-center">
    <%= button_to "Continuer mes achats", books_path(@category), class: "site-btn-second", data: { turbo: false } %>
  </div>
<% else %>
<div class="text-center">
    <%= button_to "Continuer mes achats", books_path(@category), class: "site-btn", data: { turbo: false } %>
  </div>
<% end %>

