<section class="product-page spad m-1">
  <div class="container">
    <div class="row">
      <div class="col-lg-12">
        <div class="product__page__content">
          <div class="product__page__title">
            <div class="row">
              <div class="col-lg-8 col-md-8 col-sm-6">
                <div class="section-title">
                  <h4>Evenements</h4>
                </div>
              </div>
            </div>
          </div>
          <% if @events %>
            <% if @events.any? %>
              <ul>
                <% @events.each do |event| %>
                  <%= link_to "#{event.title} le #{event.start_date}", event_path(event) %>
                  
                <% end %>
              </ul>
            <% else %>
              <p>Pas d'evenements trouvés</p>
            <% end %>
          <% end %>
        </div>
      </div>
    </div>
  </div>
</section>